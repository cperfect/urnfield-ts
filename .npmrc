; The @cperfect scope is published to the public npm registry, not GitHub
; Packages. Declaring it explicitly stops Dependabot from assuming an
; owner-scoped package lives on npm.pkg.github.com (which it then can't auth to).
; Contains no credentials — auth is provided by CI/your own ~/.npmrc.
@cperfect:registry=https://registry.npmjs.org/
